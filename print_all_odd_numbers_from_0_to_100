# Create a loop that iterates through numbers from 0 to 100.
for number in range(101):  # Range goes up to (but not including) 101

    # Check if the current number is odd.
    if number % 2 != 0:

# If it's odd, print the number.